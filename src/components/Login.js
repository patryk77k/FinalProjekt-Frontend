import { NavLink } from "react-router-dom";
export default function Login() {
  return (
    <div>
      <h2>Login</h2>
      <nav>
        <NavLink to="/">Home</NavLink>
      </nav>
    </div>
  );
}
